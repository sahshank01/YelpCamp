<%- include("partials/header")%>
<div class="container">
  <h3><%=campground.name%></h3>
  <img src="<%=campground.image%>" alt="Unable to load image">
  <p><%=campground.description%></p>
  <% campground.comments.forEach((comment)=>{ %>
    <div>
      <p><strong><%= comment.author%></strong> - </strong><%= comment.text %> </p>
    </div>
 <% }) %> 
</div>
<%- include("partials/footer")%>